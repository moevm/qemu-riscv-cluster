# Docker-Compose Configuration for Loki Logging

First, start the [logging service](../log_service/)


Build and run:
```
docker compose build && docker compose up
```

To stop:

```
docker compose down
```